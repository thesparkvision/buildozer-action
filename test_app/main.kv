BoxLayout:
    orientation: "vertical"

    Label:
        id: hello_label
        text: "Hello World 2"
        on_text: if len(self.text) > 30: self.text = "Stop clicking!"

    Button:
        text: "Click me!"
        on_release: hello_label.text += "!"
